# Dependency cooldown: refuse versions published less than 7 days ago.
# Compromised releases are typically yanked within hours of publication, so a
# short delay filters most supply-chain incidents at the install layer rather
# than after they are already in the lockfile. Requires npm >= 11.10.0.
min-release-age=7
